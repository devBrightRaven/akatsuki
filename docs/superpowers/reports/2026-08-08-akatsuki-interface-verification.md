# Akatsuki Interface Verification - Task 7

**Date:** 2026-08-08  
**Artifact:** the generated `public/` output in the isolated worktree  
**Server:** `http://127.0.0.1:8081/` served from the worktree without vault sync

## Static precondition

`pnpm run check` passed after removing the stale ignored `public/assets/presentation.js` generated by an earlier build. `git diff --check` passed. The verifier generated 76 files and reported `Akatsuki build verification passed`.

## Browser matrix

The local Playwright run covered 192 cases:

- widths: 320, 768, 1024, 1280, 1365, 1440, 1742, 1920
- surfaces: Latest, Series, Topic map, Article
- locales: EN, JA, zh-TW
- themes: Light, Dark

Every case passed these assertions: the document and rendered elements had no horizontal overflow; the selected theme was applied; denied consent hid the banner; the H1 was visible; and the Topic map details were open. The desktop Latest grid resolved to two columns at 1280px. No console errors were observed in this run. The current Topic map fixture contains nine article links and six visible connected-topic anchors, all resolving to another topic node.

## Interaction evidence

| Check | Status | Evidence |
| --- | --- | --- |
| First visit at 320x568 | PASS | Consent banner measured 227.2px, exactly 40% of the viewport. The page H1, consent heading, privacy link, and both actions were visible. |
| Initial keyboard entry | PASS | On a fresh visit, the non-modal banner does not move focus. Normal document tab order remains intact; reopening from `Privacy choices` focuses `br-consent-title`. |
| Before consent | PASS | Browser request listener captured zero measurement-provider requests before a choice; the consent panel exposed a polite live region announcing the undecided state. |
| Choice and persistence | PASS | Choosing No thanks or allowing measurement immediately hides the panel, persists the choice in local storage, and avoids a second close action. The close button remains available when reopening privacy choices without changing the current choice. |
| Privacy re-entry | PASS | Footer entry reopened the banner and moved focus to `br-consent-title`; close returned focus to the footer trigger. |
| Acceptance | PASS | Acceptance created one provider runtime and one `page_view` event path. The request log captured the provider bootstrap and collection traffic only after acceptance. |
| Withdrawal | PASS | Withdrawal removed the provider runtime and produced no new measurement request. The UI states that already transmitted data cannot be retracted. |
| Theme keyboard control | PASS | ArrowDown applied Light and then Dark immediately, updating the stored value, visible label, and sun/moon icon. Escape closed the popover. |
| Responsive navigation alignment | PASS | At wrapped-header widths, the complete control row is centered; at 320px and 480px the content navigation is centered within its row. The 192-case matrix found zero horizontal overflow. |
| Raven | PASS | Hidden at the top; visible after scrolling beyond one viewport while the footer privacy control was below the viewport; hidden again when the footer control entered view to avoid overlap. |
| Topic map | PASS | Three populated native `details` nodes were open by default. Each node showed its connected topics as anchors to the other nodes, plus its related article links in one semantic list; local runtime counted 6 connection anchors, 9 article links, and all connection targets existed. |

## Screenshots

- [Latest EN, 1280px, Light](screenshots/latest-en-1280-light.png)
- [Topic map EN, 1280px, Dark](screenshots/topics-en-1280-dark.png)
- [Article JA, 768px, Light](screenshots/article-ja-768-light.png)
- [First consent visit zh-TW, 320x568, Light](screenshots/consent-zh-320-light.png)

The Topic map screenshot was recaptured after the relationship-anchor correction; the other screenshots remain representative of the same build.

## Coverage limits

- Consent was visually captured at 320x568. The requested 390x844 and 768x1024 consent screenshots were not separately captured.
- Browser UI 200% zoom, text-spacing override, forced-colors runtime, JavaScript-disabled reading, and a fresh local-storage failure context were not directly exercised.
- No screen reader, non-Chromium browser, production-site, or external provider delivery test was performed.
- Screenshot review is representative, not a screenshot for every one of the 192 matrix cases.

The build and local runtime evidence are sufficient for the implementation handoff, but the coverage limits above remain before an accessibility or production-release claim.

## Release retest - 2026-08-15

- `pnpm run check` cleaned stale output, generated 21 public files, and passed the build verifier. Draft sources are excluded from output and collections; the verifier fails if any draft HTML is generated.
- A fresh 168-case matrix covered the seven required widths, three locales, four surfaces, and Light/Dark. Every page returned 200 with the expected theme and H1; document and body overflow were zero.
- Seven zh-TW Latest screenshots were visually checked at 320, 768, 1024, 1280, 1440, 1742, and 1920 pixels with no clipped text, horizontal scrolling, compressed reading column, or viewport-sized dead space.
- The first-visit consent focus bug found during release review was corrected. A clean reload kept focus on `BODY`, the first Tab reached the skip link, no measurement-provider request occurred before choice, and reopening from `Privacy choices` focused `br-consent-title`.
- After the draft-output correction, the same 168-case matrix passed again. Published article routes returned 200; the sampled English and zh-TW draft routes returned 404.
